# Late-night P0 duty card — 2026-03-20

## 1. 现在第一步点哪
- 先只点 **Job Toolkit / Ko-fi** 首动：核 **`$29 one-time`**、无 **`PWYW / or more`**、`redirect / exact buy path` 正常、`Title + Summary` 已在公开首屏生效。
- 这 4 项没 clean 前，今晚不切别的。

## 2. 什么条件下继续到 AINave
- 仅当上面 4 项都 clean，且仍走 **`ResumeForge -> /ai-resume/`** 这条老路。
- 现场仍满足：登录态还在、页面仍是 **`Submit a Product`**、`/ai-resume/` 仍清楚匹配 **free / live preview / PDF export / ATS-safe**。
- 满足就切 **AINave**；今晚只到 AINave，先不顺手带 The Rundown。

## 3. 什么条件下立刻停
- `price / redirect / exact buy path` 任一不确定；或 `Title / Summary` 公开首屏回漂/失真。
- AINave 一旦出现 `login / register / verify`、`form changed`、要新素材/上传、无确认/报错，立刻停。
- `/ai-resume/` 不再匹配当前 framing，也立刻停。

## 4. 今晚不要重开什么
- 不重开 **`Freelancer / Content Creator / LinkedIn`** SKU 队列。
- 不重开 **Pricing / 服务页 / 新页 / 新 offer**，不补 bullets / CTA，不写 parity/debug 卡。
- 不重开 **The Rundown / AI Tools Inc / Reddit / Future Tools / 2-24 旧任务**；深夜只保 **`Job Toolkit clean -> AINave`**。

## 5. Codex QA 监控清单
- **是否卡住：** 超过 20 分钟没有新的 verdict / 文件改动 / 下一步卡，就按卡住处理。
- **是否越界：** 一旦开始碰其他 SKU、把 Pricing / 服务页抬回前排、写 parity/debug 卡、扩外部分发，就算越界。
- **是否产出：** 本轮至少交付 1 个东西：`clean / not-clean` 结论 + 阻塞点，或实际改动/commit，或下一棒可直接执行的一屏卡。

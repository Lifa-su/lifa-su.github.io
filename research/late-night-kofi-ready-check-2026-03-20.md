# Late-night Ko-fi ready check — 2026-03-20

1. **Job Toolkit Ko-fi 还是当前第一动作：是。**
   - 深夜只认 `Job Toolkit / Ko-fi` 为收钱首动；但**只有 `green/go` 已明确**才进场。
   - 如果 `green/go` 不明或红灯，今晚就**整链停**，不外扩到别的 SKU。

2. **前 10 分钟只允许动这 4+1 个字段**
   1. `Buy box / price mode`：锁死 **`$29 one-time`**，清掉 **`PWYW / or more`**
   2. `Redirect / buy path`
   3. `Title / Product name`
   4. `Product summary`
   5. `Short description` 开头 1–2 句（**仅剩余时间才动**）
   - **不动** bullets / CTA / image / delivery / 其他 SKU

3. **Red-stop：出现任一项立刻停**
   - `green/go` 不明确，或 `price / PWYW / redirect` 任一冲突
   - 保存后公开页首屏里 **`$29` / title / summary** 没实锤生效，或任一回漂
   - `buy path` 不确定、错路、需要现场猜

4. **Ko-fi clean 后立即交接到这一步**
   - 立刻切 **AINave**
   - 只接最小分发动作：`ResumeForge → /ai-resume/`
   - 今晚先不往 `The Rundown / AI Tools Inc / Reddit` 滚